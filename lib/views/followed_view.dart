import 'package:flutter/material.dart';

class FollowedView extends StatelessWidget {
  const FollowedView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Followed View'),
      ),
    );
  }
}
